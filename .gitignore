# lemoncheck
*.html
!docs/*.html
lemoncheck-report-*.html
.DS_Store
*.log
/tmp/
scratchpad/

# Build artifacts: keep the single-file script (curl one-liner needs it in the
# repo), but the .app/.dmg are large binaries — ship them as release assets.
dist/*.app/
dist/*.dmg
